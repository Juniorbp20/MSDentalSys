@model MSDentalSys.Web.Models.ViewModels.SeguroFormViewModel
@{
    ViewData["Title"] = "Editar seguro";
}

<div class="service-page">
    <div class="page-heading">
        <div>
            <p class="dashboard-eyebrow">Cat&aacute;logo administrativo</p>
            <h1>Editar seguro m&eacute;dico</h1>
            <p>Actualiza el nombre de la aseguradora.</p>
        </div>
        <a asp-action="Index" class="btn btn-light">Volver</a>
    </div>

    <div class="service-form-card">
        <form asp-action="Edit" asp-route-id="@Model.SeguroId" method="post">
            <input asp-for="SeguroId" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
            <div class="mb-3">
                <label asp-for="Nombre" class="form-label"></label>
                <input asp-for="Nombre" class="form-control" autocomplete="off" />
                <span asp-validation-for="Nombre" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-ms-primary">Guardar cambios</button>
            <a asp-action="Details" asp-route-id="@Model.SeguroId" class="btn btn-light">Cancelar</a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
